import { describe, test } from "@jest/globals";

describe('For conversations', () => {
    test('should create a conversation', () => {

    });
    test('should get a conversation', () => {

    });
    test('should get user conversations', () => {

    });
    test('should send a test message in the conversation', () => {

    });
    test('should throw error on creating self conversation', () => {

    })
})