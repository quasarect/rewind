import { test, describe } from "@jest/globals";

describe("For spotify", () => {
	test("should refresh the token", () => {});
	test("should throw an error on random code", () => {});
	test("should throw an error on empty code", () => {});
});
