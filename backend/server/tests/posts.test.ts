import { describe, test } from "@jest/globals";

describe('For posts', () => {
    test('should get a post from postId', () => {

    });
    test('should get a post liked by user by postId', () => {

    });
    test('should create post', () => {

    });
    test('should throw error on invalid post',()=>{

    });
    test('should reshare a post',()=>{

    });
    test('should throw an error for resharing the post again',()=>{

    });
    test('should delete a post',()=>{

    });
    test('should fetch posts made by user',()=>{

    });
    test('should fetch all posts',()=>{

    });
    test('should like a post',()=>{

    });
    test('should unlike a post',()=>{

    });
    test('should throw error on liking already liked post',()=>{

    });
    test('should throw error on unliking already unliked post',()=>{

    });
    test('should fetch posts',()=>{

    });
    
})