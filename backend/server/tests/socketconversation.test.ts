import { describe, test } from "@jest/globals";

describe('For using sockets for real time chatting', () => {
    test('should receive typing event', () => {

    });
    test('should recieve join the room event', () => {

    });
    test('should recieve a message event', () => {

    });
    test('should recieve left the room event', () => {

    });
})